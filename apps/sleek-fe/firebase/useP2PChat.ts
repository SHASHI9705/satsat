// useP2PChat removed. Keep file as placeholder to avoid missing import errors.
export function useP2PChat() {
  // Chat functionality removed per user request.
  return { messages: [], loading: false, sendMessage: async () => {} };
}
